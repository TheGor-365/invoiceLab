<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= t("app.name") %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag "application", "data-turbo-track": "reload" %>
    <%= vite_stylesheet_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/"><%= t("app.name") %></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div id="nav" class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item"><%= link_to t("nav.home"), root_path, class: "nav-link" %></li>
            <% if user_signed_in? %>
              <li class="nav-item"><a class="nav-link disabled" href="#"><%= t("nav.clients") %></a></li>
              <li class="nav-item"><a class="nav-link disabled" href="#"><%= t("nav.invoices") %></a></li>
            <% end %>
          </ul>
          <ul class="navbar-nav">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"><%= t("nav.language") %></a>
              <ul class="dropdown-menu dropdown-menu-end">
                <% %i[en ru es].each do |l| %>
                  <li><%= link_to l.upcase, url_for(locale: l), class:"dropdown-item" %></li>
                <% end %>
              </ul>
            </li>
            <% if user_signed_in? %>
              <% if current_user.admin? %>
                <li class="nav-item"><%= link_to t("nav.admin"), sidekiq_web_path, class:"nav-link" %></li>
              <% end %>
              <li class="nav-item"><%= link_to t("nav.sign_out"), destroy_user_session_path, method: :delete, class:"nav-link" %></li>
            <% else %>
              <li class="nav-item"><%= link_to t("nav.sign_in"), new_user_session_path, class:"nav-link" %></li>
              <li class="nav-item"><%= link_to t("nav.sign_up"), new_user_registration_path, class:"nav-link" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <main class="container py-4">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>
  </body>
</html>
