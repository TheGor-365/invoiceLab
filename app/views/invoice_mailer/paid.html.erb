<h3><%= t("mailers.invoice.paid.greeting", name: @invoice.client.name.presence || @invoice.client.email) %></h3>
<p><%= t("mailers.invoice.paid.body_html",
  number: @invoice.number,
  amount: sprintf("%s %.2f", @invoice.currency, @invoice.total_cents.to_f / 100.0)) %></p>
<p><%= t("mailers.invoice.common.signature", app: t("app.name")) %></p>
