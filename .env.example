# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/invoicer_saas_development

# Redis
REDIS_URL=redis://localhost:6379/0

# SMTP (локально — Mailpit/Mailhog; в проде — реальный SMTP провайдер)
SMTP_ADDRESS=localhost
SMTP_PORT=1025
SMTP_DOMAIN=localhost
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=noreply@invoicer.local

# Stripe (placeholder - заполним на Итерации 4)
STRIPE_PUBLIC_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PRICE_FREE=
STRIPE_PRICE_STARTER=

# Rails
RAILS_MASTER_KEY=
